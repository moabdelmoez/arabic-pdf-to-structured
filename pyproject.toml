[project]
name = "arabic-demo"
version = "0.1.0"
description = "Convert Arabic/RTL PDF documents to AI-ready structured formats"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "markdown>=3.10.2",
    "opendataloader-pdf[hybrid]>=2.2.1",
    "streamlit>=1.56.0",
]
